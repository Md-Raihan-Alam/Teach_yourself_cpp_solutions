#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b;
    cout<<"Enter two number:";
    cin>>a>>b;
    cout<<"The result of "<<a<<" to the power of the "<<b<<" is "<<pow(a,b);
    return 0;
}